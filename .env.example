# TierBoard Environment Variables
# Copy this file to .env and fill in your values

# Cloudflare Workers (optional - auto-detected from wrangler login)
# Get your account ID from Cloudflare Dashboard → Workers & Pages
CLOUDFLARE_ACCOUNT_ID=your-account-id-here

# API Token (optional - use wrangler login instead)
# Create token at: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Workers (Edit), KV Storage (Edit)
CLOUDFLARE_API_TOKEN=your-api-token-here

# ===========================================
# TURN Server Configuration (for better P2P connectivity)
# ===========================================
# By default, the app uses Google STUN servers (free, no config needed).
# STUN works for most cases but may fail with symmetric NATs/strict firewalls.
#
# For better reliability, add Cloudflare TURN (1TB free/month):
# 1. Go to: https://dash.cloudflare.com/?to=/:account/calls
# 2. Create a TURN token with your domain
# 3. Copy the credentials below
#
# Leave these empty to use STUN-only mode (default)
CLOUDFLARE_TURN_TOKEN=your-cloudflare-turn-token-here
CLOUDFLARE_TURN_USERNAME=your-cloudflare-turn-username-here

# Application Settings (optional)
# Default values are used if not set
# MAX_PEERS_PER_ROOM=10
# ROOM_TTL_MS=3600000
